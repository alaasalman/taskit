Basic Installation
==================
There is no need for installation for this application so far. Being a python application, you can just launch the application by issuing ./taskit.py

The sqlite database tasklist.db is created within the source application.